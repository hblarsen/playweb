# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
# GET     /                           controllers.ConfiguratorManager.index
# Map static resources from the /public folder to the /assets URL path
# GET     /assets/*file               controllers.Assets.versioned(path="/public", file)

# Configurator
GET    /product/features/list                controllers.ProductConfigurator.list
GET    /product/features/init                controllers.ProductConfigurator.init

# TODO Services
# POST /product/config/save                  controllers.ProductConfiguration.save
# POST /product/config/validate              controllers.ProductConfiguration.validate
# GET  /product/config/load                  controllers.ProductConfiguration.load