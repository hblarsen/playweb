package models.Rules

// This class should build the model of the rules so that product configurations can be validated

class RuleFactory {

  def confval(jr: JRule): Boolean = {
    //val rif: Seq[String] = jr.rif
    //val rthen: Seq[String] = jr.rthen
    //for(jrule <- rif){
    //  if(jrule.contains("AND")){
    //  rif.head.contains("AND")
    //  }
    //}
    true
  }

  //def
}
